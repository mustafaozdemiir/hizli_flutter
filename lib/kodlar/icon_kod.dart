import 'package:flutter/material.dart';

class IconKod extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Icon(Icons.account_box),
    );
  }
}
