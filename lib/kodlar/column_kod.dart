import 'package:flutter/material.dart';
class ColumnKod extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: <Widget>[
        Icon(Icons.looks_one),
        Icon(Icons.looks_two),
        Icon(Icons.looks_3),
      ],
    );
  }
}
