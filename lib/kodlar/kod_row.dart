import 'package:flutter/material.dart';
class RowKod extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Row(
      children: <Widget>[
        Icon(Icons.looks_one),
        Icon(Icons.looks_two),
        Icon(Icons.looks_3),
      ],
    );
  }
}
